/**
 * An elaboration on the terminal-menu package, allowing easier "page"
 * definition, allowing for inert text, selectable options, and checkbox
 * options that can be toggled on-page.
 */

// we simply return the Program constructor
module.exports = { Program: require("./program") };
